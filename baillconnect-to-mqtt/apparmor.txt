#include <tunables/global>

profile baillconnect_to_mqtt flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # The bridge only needs outbound HTTP(S) to Baillconnect and MQTT to the broker.
  network inet stream,
  network inet6 stream,

  signal (send,receive) set=(term,int,hup,cont),

  # Home Assistant base images start CMD through S6.
  /init rix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,

  /app/baillconnect-to-mqtt rix,
  /data/options.json r,

  /etc/hosts r,
  /etc/nsswitch.conf r,
  /etc/resolv.conf r,
  /etc/ssl/certs/** r,
  /etc/localtime r,

  /tmp/** rwk,
}
